// just some
// comments before the
// actual spec
describe("a spec with leading comments", function() {
    it("works", function() {
        expect(1).toBe(1);
    });
    describe("ignore this nested describe", function() {
    });
});
